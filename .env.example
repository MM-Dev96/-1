# A single key is enough. The app can also accept comma-separated keys.
GEMINI_API_KEY=
# GEMINI_API_KEYS=key_one,key_two

# Optional overrides.
GEMINI_MODEL=gemini-2.5-flash
PORT=3000

# Use deterministic local outputs without calling Gemini.
# AI_PROVIDER=mock
